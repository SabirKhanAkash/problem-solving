#include<stdio.h>
int main()
{
    int N,factorial(=1,i;
    scanf("%d",&N);
    if (N<=1)
        printf("1\n");
    else if (N<=12)
    {
        for (i=1 ; i<=N ; i++)
        {
            f*=i;
        }
        printf("%d\n",f);
    }

    return 0;
}
